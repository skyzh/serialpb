#include "serialpb.h"
using namespace std;

vector<string> SerialProtocol::on_packet_recv(const string& data) {

}

string SerialProtocol::send_packet(const string& data) {
    string result;
    return move(result);
}
